interface:
  display_name: "Describe PR"
  short_description: "Create or update a PR description for the current branch"
  default_prompt: "Use $describe-pr to create a PR for the current branch or update an existing PR's description."
policy:
  allow_implicit_invocation: true
