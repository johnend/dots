---
description: Debug and fix failing tests
---

# /test-fix

Debug and fix failing tests. Delegated to Artificer.

1. Identify which tests are failing and why (run test suite, read output)
2. Analyze root cause (test issue vs implementation issue)
3. Fix the implementation or test as appropriate
4. Re-run tests to verify the fix — don't declare success until tests pass
