{
  "name": "opencode-todo-enforcer",
  "version": "1.0.0",
  "description": "Automatic multi-step task detection and todo enforcement for OpenCode",
  "main": "dist/cli.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "test": "node --test dist/**/*.test.js"
  },
  "keywords": ["opencode", "todo", "enforcement", "task-detection"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
