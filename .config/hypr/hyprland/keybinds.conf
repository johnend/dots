###################
### MY PROGRAMS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/

# Variables
$HYPER = CTRL SUPER ALT SHIFT

# Variables for applications
$terminal = kitty
$terminal2 = alacritty
$fileManager = XDG_CURRENT_DESKTOP=kde dolphin
$settings = XDG_CURRENT_DESKTOP=kde systemsettings
$menu = wofi --show drun
$browser = firefox
$privateBrowser = firefox --incognito
$spotify = spotify-launcher
$walls = $HOME/.config/hypr/swww/walls.sh

####################
### KEYBINDINGSS ###
####################

#System
bind = SUPER, X, exit,
bind = SUPER, L, exec, hyprlock

# UI stuff
bind = SUPER CTRL, W, exec, $walls

# Launchers
# bind = SUPER, T, exec, $terminal
bind = SUPER, T, exec, $terminal2
bind = SUPER, E, exec, $fileManager
bind = SUPER, I, exec, $browser
bind = SUPER SHIFT, I, exec, $privateBrowser
bind = SUPER, M, exec, $spotify
bind = SUPER, R, exec, $menu
bind = SUPER, A, exec, ags
bind = SUPER SHIFT, A, exec, ags -q

bind = SUPER ALT, S, exec, $settings

# Close active window
bind = SUPER, Q, killactive,

# Tiling
bind = SUPER, F, togglefloating,
bind = SUPER, P, pseudo, # dwindle
bind = SUPER, J, togglesplit, # dwindle
bind = SUPER SHIFT, M, fullscreen, 1
bind = SUPER ALT, M, fullscreen

# Layout
bind = $HYPER, Y, swapsplit 

# Window navigation
bind = $HYPER, H, movefocus, l
bind = $HYPER, J, movefocus, d
bind = $HYPER, K, movefocus, u
bind = $HYPER, L, movefocus, r

# Switch workspaces with mainMod + [0-9]
bind = SUPER, 1, workspace, 1
bind = SUPER, 2, workspace, 2
bind = SUPER, 3, workspace, 3
bind = SUPER, 4, workspace, 4
bind = SUPER, 5, workspace, 5
bind = SUPER, 6, workspace, 6

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = SUPER SHIFT, 1, movetoworkspace, 1
bind = SUPER SHIFT, 2, movetoworkspace, 2
bind = SUPER SHIFT, 3, movetoworkspace, 3
bind = SUPER SHIFT, 4, movetoworkspace, 4
bind = SUPER SHIFT, 5, movetoworkspace, 5
bind = SUPER SHIFT, 6, movetoworkspace, 6

# Example special workspace (scratchpad)
bind = SUPER, S, togglespecialworkspace, magic
bind = SUPER SHIFT, S, movetoworkspace, special:magic
bind = SUPER CTRL, S, togglespecialworkspace, magic1
bind = SUPER CTRL SHIFT, S, movetoworkspace, special:magic1

# Scroll through existing workspaces with mainMod + scroll
bind = SUPER, mouse_down, workspace, e+1
bind = SUPER, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = SUPER, mouse:272, movewindow
bindm = SUPER, mouse:273, resizewindow

# Audio control binds
bind = , XF86AudioRaiseVolume, exec, wpctl set-volume -l 0.5 @DEFAULT_AUDIO_SINK@ 5%+
bind = , XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bind = , XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bind = , XF86AudioPrev, exec, mediactl Previous
bind = , XF86AudioPlay, exec, mediactl PlayPause
bind = , XF86AudioNext, exec, medaictl Next
